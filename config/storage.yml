# Local filesystem
local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

spec:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

# MinIO for development
minio:
  service: S3
  endpoint: <%= ENV.fetch("MINIO_ENDPOINT", "http://127.0.0.1:9000") %>
  # Explicit local defaults prevent the AWS SDK from probing cloud instance
  # metadata when development credentials have not been configured.
  access_key_id: <%= ENV.fetch("MINIO_ACCESS_KEY", "minioadmin") %>
  secret_access_key: <%= ENV.fetch("MINIO_SECRET_KEY", "minioadmin") %>
  region: us-east-1
  bucket: <%= ENV.fetch("MINIO_BUCKET", "peergem-development") %>
  force_path_style: true

# Cloudflare R2 for production
cloudflare:
  service: S3
  endpoint: <%= ENV["R2_ENDPOINT"] %>
  access_key_id: <%= ENV["R2_ACCESS_KEY"] %>
  secret_access_key: <%= ENV["R2_SECRET_KEY"] %>
  region: auto
  bucket: <%= ENV["R2_BUCKET"] %>
  force_path_style: true
